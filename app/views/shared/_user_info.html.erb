<a href="<%= geek_path(current_user) %>">
  <%= gravatar_for current_user, 70 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "view my profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.plays.count, "Stuff") %> to play with
</span>
